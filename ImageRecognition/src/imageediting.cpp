#include "../include/imageediting.h"

void imageediting(cv::Mat src, std::string settings_json) {
  //::getRotationMatrix2D((cols/2,rows/2),90,1);

  // gpu::rotate(const GpuMat& src, GpuMat& dst, Size dsize, double angle,
  // double xShift=0, double yShift=0, int interpolation=INTER_LINEAR, Stream&
  // stream=Stream::Null());
}
